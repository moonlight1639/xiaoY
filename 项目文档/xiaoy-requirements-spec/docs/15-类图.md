# 小Y系统 UML 类图

> 基于源码结构整理，分为：系统整体类图（前后端）、实体类图、三层架构类图、AI 服务类图。

---

## 0. 系统整体类图（前后端）

表示系统核心数据模型、前端架构与后端架构之间的完整类关系。

### 0.1 核心数据模型（前后端共享）

前端 TypeScript 接口与后端 Java 实体字段一一对应，通过 HTTP JSON 传输。

```mermaid
classDiagram
    class User {
        <<Model>>
        +id: number
        +username: string
        +password: string
        +userinfoId: number
        +createTime: string
        +updateTime: string
    }

    class UserInfo {
        <<Model>>
        +id: number
        +nickname: string
        +phone: string
        +email: string
        +gender: number
        +avatar: string
        +userType: number
        +userStatus: number
    }

    class Course {
        <<Model>>
        +id: number
        +courseName: string
        +teacher: string
        +description: string
        +content: string
        +avatar: string
        +readCount: number
        +likeCount: number
        +commentCount: number
    }

    class CourseComment {
        <<Model>>
        +id: number
        +courseId: number
        +userId: number
        +userName: string
        +content: string
        +likeNum: number
        +dislikeNum: number
    }

    class Dish {
        <<Model>>
        +id: number
        +dishName: string
        +price: number
        +category: string
        +locationId: number
        +locationName: string
        +photo: string
    }

    class CanteenLocation {
        <<Model>>
        +id: number
        +name: string
    }

    class ChatForm {
        <<Model>>
        +memoryId: string
        +content: string
    }

    class ChatMessage {
        <<Model>>
        +memoryId: string
        +title: string
        +type: string
        +content: string
        +createTime: string
    }

    User "1" --> "1" UserInfo : 关联
    Course "1" --> "0..*" CourseComment : 包含评论
    UserInfo "1" --> "0..*" CourseComment : 发布评论
    CanteenLocation "1" --> "0..*" Dish : 包含菜品
    ChatForm ..> ChatMessage : 产生
```

### 0.2 前端架构类图

```mermaid
classDiagram
    class AuthStore {
        <<Store>>
        +user: UserInfo
        +token: string
        +isAuthenticated: boolean
        +setUser(user, token): void
        +logout(): void
    }

    class AppStore {
        <<Store>>
        +theme: string
        +loading: boolean
        +setTheme(theme): void
        +toggleTheme(): void
        +setLoading(loading): void
    }

    class UserApiService {
        <<Service>>
        +login(user): Promise
        +register(user): Promise
        +getUserInfo(id): Promise
        +updateUserInfo(params): Promise
    }

    class CourseApiService {
        <<Service>>
        +getCourses(page, size): Promise
        +getCourseById(id): Promise
        +saveCourse(course): Promise
        +updateCourse(course): Promise
    }

    class ChatApiService {
        <<Service>>
        +getChatList(): Promise
        +getMessages(memoryId): Promise
        +streamingChat(chatForm): EventSource
        +getNewMemoryId(chatForm): Promise
    }

    class HttpClient {
        <<Axios>>
        +baseURL: string
        +timeout: number
        +get(url): Promise
        +post(url, data): Promise
    }

    AuthStore --> UserApiService : 调用登录/注册
    UserApiService --> HttpClient : HTTP请求
    CourseApiService --> HttpClient : HTTP请求
    ChatApiService --> HttpClient : HTTP请求
    AuthStore ..> UserInfo : 持有
```

### 0.3 后端架构类图

```mermaid
classDiagram
    class UserController {
        <<Controller>>
        +list(pageNum, pageSize): Result
        +register(user): Result
        +login(user): Result
        +info(id): Result
        +save(user): Result
        +update(user): Result
        +delete(ids): Result
    }

    class CourseController {
        <<Controller>>
        +list(pageNum, pageSize): Result
        +info(id): Result
        +save(course): Result
        +update(course): Result
        +delete(ids): Result
    }

    class ChatController {
        <<Controller>>
        +getList(): Result
        +getMessages(memoryId): Result
        +streamingChat(chatForm): Flux
        +xiaoY_chat(chatForm): Result
        +getNewMemoryId(chatForm): Result
    }

    class UserService {
        <<Interface>>
        +queryPage(pageNum, pageSize): List
        +register(user): Result
        +login(user): Result
    }

    class CourseService {
        <<Interface>>
        +queryPage(pageNum, pageSize): List
    }

    class ChatService {
        <<Interface>>
        +streamingChat(chatForm): Flux
        +xiaoY_chat(chatForm): ChatMessagesVO
        +getList(): List
        +getMessages(memoryId): ChatMessagesList
    }

    class UserMapper {
        <<Mapper>>
    }

    class CourseMapper {
        <<Mapper>>
    }

    UserController --> UserService : 依赖
    CourseController --> CourseService : 依赖
    ChatController --> ChatService : 依赖
    UserService --> UserMapper : 依赖
    CourseService --> CourseMapper : 依赖
    UserMapper --> User : 操作
    CourseMapper --> Course : 操作
```

### 0.4 前后端交互关系

```mermaid
classDiagram
    class 前端页面层 {
        <<Frontend>>
        +ChatPage
        +Courses
        +Canteen
        +AdminUsers
    }

    class 前端服务层 {
        <<Frontend>>
        +courseApiService
        +chatApiService
        +userApiService
        +dishApiService
    }

    class 前端状态层 {
        <<Frontend>>
        +AuthStore: 用户认证状态
        +AppStore: 应用全局状态
    }

    class 后端Controller层 {
        <<Backend>>
        +UserController
        +CourseController
        +ChatController
        +DishController
    }

    class 后端Service层 {
        <<Backend>>
        +UserService
        +CourseService
        +ChatMessageByUserService
    }

    class 后端数据层 {
        <<Backend>>
        +MySQL: 用户/课程/菜品
        +MongoDB: 聊天记录
        +Redis: 缓存/Session
        +MinIO: 文件存储
    }

    前端页面层 --> 前端服务层 : 调用API
    前端页面层 --> 前端状态层 : 读写状态
    前端服务层 --> 后端Controller层 : HTTP/JSON
    后端Controller层 --> 后端Service层 : 业务处理
    后端Service层 --> 后端数据层 : 数据读写
```

---

## 1. 实体类图

展示系统核心业务实体的属性及实体间的关联关系。

### 1.1 用户相关实体

```mermaid
classDiagram
    class User {
        +id: Integer
        +username: String
        +password: String
        +userinfoId: Integer
        +createTime: Date
        +updateTime: Date
    }

    class UserInfo {
        +id: Integer
        +nickname: String
        +phone: String
        +email: String
        +gender: Integer
        +avatar: String
        +userType: Integer
        +userStatus: Integer
        +isDeleted: Integer
        +createTime: Date
        +updateTime: Date
    }

    User "1" --> "1" UserInfo : 关联 userinfoId
```

### 1.2 课程相关实体

```mermaid
classDiagram
    class Course {
        +id: Integer
        +courseName: String
        +teacher: String
        +description: String
        +content: String
        +avatar: String
        +readCount: Integer
        +likeCount: Integer
        +commentCount: Integer
        +collectCount: Integer
        +isDeleted: Integer
        +createTime: Date
        +updateTime: Date
    }

    class CourseComment {
        +id: Integer
        +courseId: Integer
        +userId: Integer
        +userName: String
        +userAvatar: String
        +content: String
        +likeNum: Integer
        +dislikeNum: Integer
        +isDeleted: Integer
        +createTime: Date
        +updateTime: Date
    }

    Course "1" --> "0..*" CourseComment : 包含评论 courseId
```

### 1.3 食堂相关实体

```mermaid
classDiagram
    class CanteenLocation {
        +id: Integer
        +name: String
        +isDeleted: Integer
        +createTime: Date
        +updateTime: Date
    }

    class Dish {
        +id: Integer
        +dishName: String
        +description: String
        +photo: String
        +price: BigDecimal
        +category: String
        +locationId: Long
        +locationName: String
        +status: Integer
        +isDeleted: Integer
        +likeCount: Integer
        +createTime: Date
        +updateTime: Date
    }

    CanteenLocation "1" --> "0..*" Dish : 包含菜品 locationId
```

### 1.4 实体关系总览

```mermaid
classDiagram
    class User
    class UserInfo
    class Course
    class CourseComment
    class CanteenLocation
    class Dish

    User "1" --> "1" UserInfo : 关联
    Course "1" --> "0..*" CourseComment : 包含评论
    UserInfo "1" --> "0..*" CourseComment : 发布评论
    CanteenLocation "1" --> "0..*" Dish : 包含菜品
```

---

## 2. 三层架构类图

展示 Controller、Service 接口、Mapper 接口之间的类层次结构和依赖关系。

### 2.1 用户模块

```mermaid
classDiagram
    class UserController {
        -userService: UserService
        +list(pageNum, pageSize): Result
        +register(user): Result
        +login(user): Result
        +info(id): Result
        +save(user): Result
        +update(user): Result
        +delete(ids): Result
    }

    class UserService {
        <<interface>>
        +queryPage(pageNum, pageSize): List
        +register(user): Result
        +login(user): Result
    }

    class UserMapper {
        <<interface>>
    }

    UserController --> UserService : 依赖
    UserService --> UserMapper : 依赖
    UserMapper --> User : 操作
```

### 2.2 课程模块

```mermaid
classDiagram
    class CourseController {
        -courseService: CourseService
        +list(pageNum, pageSize): Result
        +info(id): Result
        +save(course): Result
        +update(course): Result
        +delete(ids): Result
    }

    class CourseService {
        <<interface>>
        +queryPage(pageNum, pageSize): List
    }

    class CourseMapper {
        <<interface>>
    }

    CourseController --> CourseService : 依赖
    CourseService --> CourseMapper : 依赖
    CourseMapper --> Course : 操作
```

### 2.3 课程评论模块

```mermaid
classDiagram
    class CourseCommentService {
        <<interface>>
        +queryPage(pageNum, pageSize): List
        +getByCourseId(id): Result
        +commitComment(comment): void
    }

    class CourseCommentMapper {
        <<interface>>
    }

    CourseCommentService --> CourseCommentMapper : 依赖
    CourseCommentMapper --> CourseComment : 操作
```

### 2.4 AI 聊天模块

```mermaid
classDiagram
    class ChatMessageByUserController {
        -chatMessageByUserService: ChatMessageByUserService
        +getByMomeyId(memoryId): Result
        +getList(): Result
        +getMessages(memoryId): Result
        +xiaoY_chat(chatform): Result
        +streamingChatTest(userMessage): Flux
        +streamingChat(chatform): Flux
        +getNewMemoryId(chatform): Result
    }

    class ChatMessageByUserService {
        <<interface>>
        +getByMemoryId(memoryId): ChatMessageByUser
        +getList(): List
        +getMessages(memoryId): ChatMessagesList
        +xiaoY_chat(chatform): ChatMessagesVO
        +streamingChatTest(userMessage): Flux
        +streamingChat(chatform): Flux
        +getNewMemoryId(chatform): ChatMessagesVO
    }

    ChatMessageByUserController --> ChatMessageByUserService : 依赖
```

---

## 3. AI 服务类图

展示 LangChain4j AI 服务接口、配置类及其依赖关系。

### 3.1 AI 接口定义

```mermaid
classDiagram
    class XiaoY {
        <<AiService>>
        +chat(memoryId, userMessage): String
        +fill(userClass, userMessage): String
    }

    class xiaoYStreaming {
        <<AiService>>
        +test(userMessage): Flux
    }
```

### 3.2 AI 配置类

```mermaid
classDiagram
    class XiaoYConfig {
        <<Configuration>>
        -couresEmbeddingStore: EmbeddingStore
        -courseReviewEmbeddingStore: EmbeddingStore
        -qwenEmbeddingModel: EmbeddingModel
        +chatMemoryProvider(): ChatMemoryProvider
        +courseContentRetriever(): ContentRetriever
        +courseReviewContentRetriever(): ContentRetriever
        +multicontentRetrieverXiaoYPincone(): ContentRetriever
    }

    class ChatMemoryProvider {
        <<Bean>>
    }

    class ContentRetriever {
        <<Bean>>
    }

    class qwenChatModel {
        <<Model>>
    }

    class qwenStreamingChatModel {
        <<Model>>
    }

    XiaoYConfig ..> ChatMemoryProvider : 创建
    XiaoYConfig ..> ContentRetriever : 创建
```

### 3.3 AI 服务完整调用链

```mermaid
classDiagram
    class ChatMessageByUserController {
        +streamingChat(chatform): Flux
        +xiaoY_chat(chatform): Result
    }

    class ChatMessageByUserService {
        +streamingChat(chatform): Flux
        +xiaoY_chat(chatform): ChatMessagesVO
    }

    class XiaoY {
        <<AiService>>
        +chat(memoryId, userMessage): String
    }

    class xiaoYStreaming {
        <<AiService>>
        +test(userMessage): Flux
    }

    class ChatMemoryProvider {
        <<Bean>>
    }

    class ContentRetriever {
        <<Bean>>
    }

    class qwenChatModel {
        <<Model>>
    }

    class qwenStreamingChatModel {
        <<Model>>
    }

    ChatMessageByUserController --> ChatMessageByUserService : 调用
    ChatMessageByUserService --> XiaoY : 普通对话
    ChatMessageByUserService --> xiaoYStreaming : 流式对话
    XiaoY ..> ChatMemoryProvider : 使用
    XiaoY ..> ContentRetriever : 使用
    XiaoY ..> qwenChatModel : 使用
    xiaoYStreaming ..> ChatMemoryProvider : 使用
    xiaoYStreaming ..> ContentRetriever : 使用
    xiaoYStreaming ..> qwenStreamingChatModel : 使用
```

---

## 说明

| 符号 | 含义 |
|------|------|
| `-->` | 关联 / 依赖关系（有方向） |
| `..>` | 使用 / 依赖（虚线） |
| `"1" --> "0..*"` | 一对多关系 |
| `<<interface>>` | 接口类型 |
| `<<AiService>>` | LangChain4j AI 服务代理接口 |
| `<<Configuration>>` | Spring 配置类 |
| `<<Bean>>` | Spring 托管的 Bean 对象 |
| `<<Model>>` | AI 大模型适配器 |
